#include<stdio.h>

int main()
{
    int qty;
    float price, total;

    printf("Enter quantity: ");
    scanf("%d", &qty);

    printf("Enter price per item: ");
    scanf("%f", &price);

    total = qty * price;

    printf("\nTotal Bill = %.2f", total);

    return 0;
}