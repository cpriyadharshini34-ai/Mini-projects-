#include<stdio.h>

int main()
{
    int tickets;
    float price = 120, total;

    printf("Enter number of tickets: ");
    scanf("%d", &tickets);

    total = tickets * price;

    printf("Total Ticket Cost = %.2f", total);

    return 0;
}